# Node / frontend
node_modules/
dist/
build/
*.log

# Python
__pycache__/
*.pyc

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
~$*            # Office 临时锁文件(如 ~$deck.pptx)

# Scratch / working assets (screenshots, article drafts, store-asset exports)
tmp/

# 演示素材(二进制 pptx + 构建脚本,不入公开仓库)
ppt/

# R1 test certs
plugin/certs/

# R1 filled-in config (has vCenter host/user) — keep local
plugin/r1.env

# built plugin package
plugin/dist/

# plugin UI tests
plugin/tests/node_modules/
plugin/tests/shot-*.png
plugin/dist/_uitest/

# 官方语言包源(大文件,不入库)
vsphere-lang.tgz
vsphere-l10n.tgz
vsphere-l10n-all.tgz

# 生成的字典(dict.*.json)现随主仓库入库,直接经 jsDelivr 从主仓库分发
# (仓库已公开,不再拆单独的 langpacks 数据仓库)。构建源见 plugin/i18n/glossary.*.json。
*.tgz
